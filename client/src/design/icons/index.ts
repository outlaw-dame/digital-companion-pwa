/**
 * Icons Index
 * 
 * Export icon registry and utilities.
 * 
 * Usage:
 * import { iconRegistry, platformIcons, iconNames, isValidIcon } from '@/design/icons';
 */

export { iconRegistry, platformIcons, iconNames, isValidIcon, getIcon } from './iconRegistry';
export type { IconDefinition, IconName } from './iconRegistry';
